# 练习（按模块）

## 1. PSD 与二次型
- 证明：`A \succeq 0` 当且仅当存在 `B` 使 `A=B^T B`。
- 证明：若 `A \succeq 0` 且 `M` 任意，则 `M^T A M \succeq 0`。

## 2. 核函数判定（你要的链路）
- 设 `k(x,y)=f(x)f(y)`，按“任取点集→写 `K`→验 PSD”证明 `k` 是核函数。
- 设 `k(x,y)=k1(x,y)+k2(x,y)`，已知 `k1,k2` 是核函数，证明 `k` 仍是核函数。
