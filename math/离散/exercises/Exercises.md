# 练习（按模块）

## 1. 定义检查
- 给出一个运算，判断是否为半群/幺半群/群，并指出失败的公理。

## 2. 同态与核像
- 给定 `f: Z -> Z/nZ`，求 `ker(f)` 与 `im(f)`，写出第一同构定理对应的同构。

## 3. 陪集与商群
- 在 `Z` 中取子群 `nZ`，写出陪集并说明 `Z/nZ` 的群结构。

## 4. 环与理想
- 证明 `nZ` 是 `Z` 的理想，并写出 `Z/nZ` 的商环结构。
