# 域（Field）

## 1. 一句话
- 既能加减乘除（除 0 外）的代数系统；线性代数、代数方程的底层标配。

## 2. 接口：数据 + 公理
- 底层集合：`F`
- 运算：`+` 与 `×`
- 公理（常用版本）：
  - `(F, +)` 是阿贝尔群（单位元 0）
  - `(F\\{0}, ×)` 是阿贝尔群（单位元 1）
  - 分配律

## 3. 例子与反例
- 例子：`Q`、`R`、`C`
- 例子：有限域 `F_p = Z/pZ`（`p` 素数）
- 反例：`Z` 不是域（非零元素不一定可除）

## 4. 速查
- 域 ⇒ 整环 ⇒ 交换环 ⇒ 环
