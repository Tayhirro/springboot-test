# 算子（Operators）

## 1. 一句话
- “箭头”是泛函分析的主角：线性算子把空间之间的结构联系起来。

## 2. 有界线性算子（= 连续线性算子）
对赋范空间 `X, Y`，线性算子 `T: X -> Y`：
- **有界**：存在 `C` 使 `||Tx|| ≤ C||x||` 对所有 `x`
- 等价：`T` 连续

算子范数：
- `||T|| := sup_{||x||≤1} ||Tx||`

## 3. 速查
- 复合有界：`||S∘T|| ≤ ||S||·||T||`
- 典型后续：紧算子、谱（可在此页扩展）
