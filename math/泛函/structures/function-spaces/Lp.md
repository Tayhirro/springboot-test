# `L^p` 空间（Lebesgue Spaces）

## 1. 一句话
- 用“可积性 + p 次平均大小”度量函数，是泛函分析与 PDE 的主力函数空间。

## 2. 定义（占位）
- 给定测度空间 `(Ω, μ)`，`L^p(Ω)` 由（几乎处处相等的）可测函数等价类组成。
- 范数：`||f||_p = (∫ |f|^p dμ)^{1/p}`（`p<∞`），`||f||_∞ = ess sup |f|`。

## 3. 速查
- `1 ≤ p ≤ ∞` 时 `L^p` 是 Banach；`p=2` 时是 Hilbert。
